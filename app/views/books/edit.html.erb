<h1>Editing Book</h1>
<%= form_for(@book, url:"/books/#{@book.id}") do |f| %>
    <p>title</p>
    <%= f.text_field :title %>
    <p>Body</p>
    <%= f.text_area :body %>
    <p><%= f.submit 'Update Book' %></p>
<% end %>

<%= link_to 'show', book_path(@book.id)%><span> | </span><%= link_to 'back', "/books/index"%>